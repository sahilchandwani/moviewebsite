<%- include('partials/head', { title: movie.title + ' — Locked' }) %>

<section class="wrap locked-section">
  <div class="lock-icon">🔒</div>
  <h1>This film is locked</h1>
  <p>You haven't unlocked <strong><%= movie.title %></strong> yet.</p>
  <a href="/buy/<%= movie.slug %>" class="btn">Unlock this movie</a>
</section>

<%- include('partials/footer') %>
